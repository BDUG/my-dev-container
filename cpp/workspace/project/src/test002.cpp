#include <iostream>
int mean(int a, char b[]);

int main(int argc, char *argv[]){
    int mean(int a, char b[]);
    return 0;
}

int mean(int a, char b[]){
    int sum = 0, i, mean;
    for( i = 4; i> 0 ; i--){
        std::cin >> b[i];
        sum = sum + b[i];
        mean = sum / i;
        std::cout<<mean<<"/n";
        return 0;        
    }
}